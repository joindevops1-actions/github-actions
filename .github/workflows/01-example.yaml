name: HelloWorld
# When to trigger this pipeline. we can trigger on push or manual or someother events like creating PR,
# updating PR, creating branches, creating issues
on: [push] # triggers
jobs: # Pipeline. One workflow can have multiple pipelines
  hello:
    runs-on: ubuntu-latest # Linux OS. we can have windows or MAC
    steps: # stages
      - name: step1
        run: echo "Hello world"
      - name: step2
        run: echo "Hello GitHub actions"
    